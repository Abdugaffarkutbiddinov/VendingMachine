// namespace vendingMachine;
//
// public class VendingMachineWithLimit : VendingMachine
// {
//     private readonly uint _percent;
//
//     public VendingMachineWithLimit(uint sum, uint a, uint b, uint percent) : base(sum, a, b)
//     {
//         _percent = percent;
//     }
//
     // public override void Change()
     // {
     //     uint aCount;
     //     uint bCount;
     //     uint remainder = 0;
     //
     //     aCount = _sum / (_a + (1 - _percent) * _b / _percent);
     //
     //     if (aCount == 0)
     //     {
     //         bCount = _sum / (_a + _b);
     //         if (bCount == 0)
     //         {
     //             Console.WriteLine("Minimum bank note is higher than the sum");
     //             return;
     //         }
     //         remainder = _sum - _b * bCount;
     //         
     //     }
     // }
// }

